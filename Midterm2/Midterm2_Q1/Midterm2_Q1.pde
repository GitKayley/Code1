// Fix all the syntax errors so the code runs.

int increment; 

//numbers [] = new int [4]; 
void setup()
{
 size(100, 100); 
 numbers [] = new int [4]; 
 increment = .5; 
 
 for (int i = 0; i< 5; i++){
 {
   numbers = random(0, 10); 
 }
 
}
}

void draw()
{
  ellipse(x, y, r, r); 


void mousePressded(){
  int r ++; 
}
